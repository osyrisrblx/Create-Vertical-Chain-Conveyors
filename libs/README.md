# Development dependencies

Gradle resolves Create 6.0.10 and its compile-time dependencies from their
published Maven repositories. Third-party mod jars must not be committed here.

This directory may still be used for temporary local inspection, such as
running `javap` against an exact runtime jar; `*.jar` is intentionally ignored.
